# R27.1a — Manual Test Checklist

Run after deploy. Frontend = GoDaddy SPA; backend = narmada-backend.onrender.com.

## BUG 1 + 2 — Signup OTP + emails (STRICT)
- [ ] Sign up a new customer → redirected to `/#/customer/verify`, **not** logged in.
- [ ] OTP email arrives (subject references verification). Check spam.
- [ ] Welcome email arrives **after** successful verification.
- [ ] Enter wrong OTP → error, not verified.
- [ ] Enter correct OTP → auto-logged-in, lands on account (or checkout if `?next=checkout`).
- [ ] Try to log in with a brand-new unverified account → blocked with "verify" prompt (403), fresh OTP re-sent.
- [ ] Resend OTP: works; 4th resend within an hour is rate-limited.
- [ ] Existing R27.1 customer (pre-upgrade) can still log in normally (grandfathered).

## BUG 3 — Session persistence
- [ ] Log in, refresh the page → still logged in.
- [ ] Open a second tab → login state reflects there too (auth-changed event).
- [ ] Add to cart while logged out → go to checkout → bounced to login with `?next=checkout` → after login/verify, returned to checkout.

## BUG 4 — Admin Web Customers detail
- [ ] Admin → Web Customers → click a customer row → detail page opens.
- [ ] Profile shows email, phone, **Email Verified Yes/No**, joined, last login.
- [ ] Addresses, Orders, Wishlist tables populate (where data exists).
- [ ] Clicking an order row opens that order's detail.
- [ ] Back link returns to Web Customers list.

## BUG 5 — Currency picker
- [ ] Public header shows ₹/$ dropdown; switching updates displayed prices.

## BUG 6 — Process PO
- [ ] Open a PO with confirmed lines (status sent/accepted/draft/partial/open) → **Process PO** button enabled.
- [ ] Click → toast confirms; confirmed lines move to `processed`; button becomes a "Processed" badge.
- [ ] PO with no confirmed lines → graceful error, no crash.

## BUG 7 — PO search + date filter
- [ ] PO list search box filters by PO #, customer, vendor, part.
- [ ] From/To date pickers narrow the list to that range (inclusive).
- [ ] Clear button resets the dates.

## BUG 8 — Freight search
- [ ] Freight Charges Manager search filters by part number and product name.

## BUG 9 — Duplicate
- [ ] PO list → Duplicate (copy icon) → new PO created (date = today) → navigates to its detail.
- [ ] Quotes list → Duplicate (copy icon) → copy opens in Team portal.

## Sidebar
- [ ] Admin sidebar shows **Web Orders** between **Quotes** and **Purchase Orders**, shopping-bag icon, routes to `/#/admin/orders`.

## Regression
- [ ] Admin / Sales / Finance / HR / Consignment logins unchanged.
- [ ] Existing PO / Quotation / consignment flows unaffected.
