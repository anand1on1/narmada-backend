# R27.1a — Audit Report

**Date:** 2026-06-21
**Base:** R27.1 (fe67007)
**Verification env:** local, `PORT=5050 DATA_DIR=/tmp/r271a-data` (fresh SQLite), `[boot] brevo configured: no`.

---

## 1. Type check
```
npx tsc --noEmit -p .   →   EXIT 0  (0 errors)
```

## 2. Build
```
npm run build           →   success (only the Vite chunk-size advisory; non-fatal)
```

## 3. Migrations (boot log)
```
[migrations] R27.1a: start
[migrations] R27.1a: shop_users.email_verified added
[migrations] R27.1a: shop_users.verify_otp added
[migrations] R27.1a: shop_users.verify_otp_expires_at added
[migrations] R27.1a: shop_users.verify_otp_sent_at added
[migrations] R27.1a: grandfathered N pre-existing shop_users as verified
[migrations] R27.1a: complete
```
- Each ALTER is wrapped in its own try/catch → idempotent (re-run logs `skip (duplicate column)`).
- Additive only. No table dropped/renamed. Existing users grandfathered to `email_verified=1` so the strict login gate cannot lock out R27.1 signups.

## 4. Endpoint / regression checks (curl)

| Check | Result |
|---|---|
| `POST /api/admin/login` (narmadamobility123) | 200, token len 64 |
| Shop signup (unverified) | `{ verify_required: true, email }`, **no token** ✓ |
| Shop login while unverified | **403 `{ error:"verify_required", email }`** ✓ |
| `POST /api/shop/verify-otp` wrong OTP | rejected ✓ |
| `POST /api/shop/verify-otp` correct OTP | 200, sets verified + issues token ✓ |
| `GET /api/shop/auth/me` post-verify | returns user, `emailVerified:1` ✓ |
| Shop login after verify | 200 with token ✓ |
| `GET /api/admin/shop-customers/:id` | `{…, emailVerified, orders[], addresses[], wishlist[] }` ✓ |
| `GET /api/admin/freight-charges?q=abc` | 200 `{ total, rows[] }` ✓ |
| `GET /api/admin/purchase-orders-v2?from=&to=` | 200 (empty list, no 500) ✓ |
| `GET /api/admin/purchase-orders-v2?q=&from=&to=` | 200 ✓ |
| `POST /api/admin/purchase-orders/:id/mark-processed` (missing PO) | 400 `{error:"PO not found"}` — no crash ✓ |
| `POST /api/admin/purchase-orders/:id/duplicate` (missing PO) | 404 `{error:"PO not found"}` — no crash ✓ |

> Note: `GET /api/admin/quotations` returned a `shipping_name` column error **on the throwaway test DB only** — that column is added by a `storage.ts` ALTER (pre-R27.1a) which the partial test DB had not run. Not introduced by R27.1a (no R27.1a change touches `storage.ts` or the quotations list query). On a real DB (prod / a DB booted through the full migration chain) the column exists and the query works.

## 5. Email behavior
- Brevo is configured via env in prod; locally it logged `configured: no`, so OTP/welcome/status emails were exercised by injecting a known OTP hash into the test DB and verifying via curl rather than via a live inbox.
- All email sends are fire-and-forget with `[email] …failed:` error logging and notification-log entries (events: `shop_signup_otp`, `shop_welcome`, `shop_order_confirm`, `shop_order_{confirmed|packed|dispatched|delivered}`).

## 6. Constraints honored
- ✓ No `publish_website` call.
- ✓ No table dropped/renamed; columns added only.
- ✓ No endpoint removed/renamed (all changes additive or in-place behavioral).
- ✓ Hash routing only; new pages at `/#/customer/verify`, `/#/admin/web-customers/:id`.
- ✓ Admin/sales/finance/HR/consignment auth untouched.
- ✓ Shop token key `narmada_shop_token` / header `x-shop-token` unchanged.
- ✓ "seller" terminology preserved in UI.

## 7. Residual risk
- Live email delivery (Brevo SMTP) could not be observed in the sandbox; logic, event keys, and error logging are in place. Verify a real OTP + welcome email lands once deployed (see test checklist).
- Strict login gate depends on the grandfather UPDATE running on the prod DB — confirmed idempotent and logged.
