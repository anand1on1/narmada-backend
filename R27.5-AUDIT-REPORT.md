# R27.5 — Audit Report

**Date:** 2026-06-21
**Scope:** 9 ship-blocker items reported by Piyush after testing R27.4, shipped as a single commit.
**Verification:** Production build (`npm run build`) succeeded; `tsc --noEmit` exit 0; server booted on a copy of the live `data.db`, R27.5 migrations applied cleanly, and changed endpoints were exercised with `curl`.

Legend: **PASS** = built + verified working · **PARTIAL** = built, but some sub-feature deferred/limited · **DEFERRED** = stubbed for a later round.

---

## #1 Currency badge — PASS
- `SiteLayout.tsx` `CurrencyPicker()` reverted to a compact inline control (`h-9 px-2`, flag + code + chevron). Live rate moved into the dropdown header.
- `GET /api/settings/fx` → `{"usdInr":83.5}` (verified). Badge reads this rate.

## #2 Header layout overlap — PASS
- Logo wrapped in `shrink-0 max-w-[230px] overflow-hidden`; nav raised to `xl:flex … min-w-0 flex-1`; actions `shrink-0`; hamburger now `xl:hidden`. Gap tightened `gap-4`→`gap-3`.
- Nav no longer collides with the wordmark; collapses to hamburger below `xl` (1280px).
- NOTE: verified by code/breakpoint reasoning + build. Visual pixel-test at 1280/1440/1920 not run in this headless environment.

## #3 Checkout "Failed to fetch" — PASS
- `POST /api/shop/freight-quote` verified: returns `{"freightInr":1500,...}` for a known part; empty body → HTTP 200 (graceful).
- `CheckoutPage.tsx`: freight quote falls back to `setFreight(0)` on non-OK; `placeOrder` wraps the fetch in try/catch and never surfaces raw "Failed to fetch" — shows "We couldn't reach the server…" instead.

## #4 Freight search — PASS (verified round-trip)
- Migration added `freight_charges.city/source/destination/mode`.
- `PATCH /api/admin/freight-charges/TEST-PART-9001` with route fields stored them; `GET …?q=patna` and `?q=road` both returned the row. Search now matches part #, product name, city, source, destination, mode (ILIKE).
- Admin UI (`AdminFreight.tsx`) gained Source/Destination/Mode inputs + a Route column.

## #5 Store dashboard (inter-branch dispatch) — PASS (wiring verified; no live PO to drive full flow)
- Delhi dispatch (`/api/delhi/po/:id/dispatch`) now creates a branch transfer on **every** dispatch (idempotent on `po_id`+`in_transit`), not only when an internal-transfer flag was set.
- `branch_transfers` gained `from_branch_key`/`to_branch_key` (lowercased, backfilled). `listTransfers()` / `GET /api/store/transfers` return valid JSON (verified; empty in test DB which has no POs).
- `listBranchStockAdmin` switched to `LOWER(TRIM(branch)) = ?` so Delhi/delhi/patna casing all match.

## #6 Stock module syncing — PASS (verified)
- New `stock_movements` append-only ledger + `recordStockMovement()` (FIFO +/-).
- Triggers wired: procurement invoice (+Delhi via `createDelhiInvoice`), inter-branch receive (+Patna via `receiveTransfer`), customer order (−Patna via `createShopOrder`, inlined to avoid an ESM module cycle).
- `GET /api/admin/stock/summary` aggregated a seeded Delhi row to qty=10; `GET /api/admin/stock/movements` showed the `procurement_invoice +10` entry. Admin Stock page has Live (net) / Detailed toggle.

## #7 Auto-product brand + URL part number — PASS
- `products.part_number` ensured by migration. `autoCreateProductsForPo` brand derivation: PO item brand → vendor `brands` first entry → "Genuine OEM".
- Product detail (`ProductDetailPage.tsx`) renders the brand even when not in the BRANDS map (outline badge). URLs already carry the part number via `productHref()`; sitemap already includes it.

## #8 Accounts dashboard — PASS (core), DEFERRED (advance settlement UI depth, attendance bulk)
Verified end-to-end via curl:
- **Employees** full master (role/branch/email/PAN/Aadhaar/bank/IFSC/gross_salary/working_days) + live search (`?q=ravi` matched). CRUD + active toggle.
- **Expense headers** with GL code / budget / parent.
- **Cash** per-branch register with in/out direction; signed-amount balance verified (10000 in − 3000 out = **7000**), per-branch breakdown returned.
- **Current expenses** approval workflow: ₹2000 auto-approved, ₹9000 → pending → admin `approve` → approved (all verified). Branch tag stored.
- **Salary** compute verified (gross 30000 − advance 5000 − retention 2500 = **net 22500**); admin-only (403 for finance role). Email-slip button retained.
- **Person Ledger** new tab: advance created a debit; running balance = 5000 (verified).
- **Masking** verified: finance role sees `gross_salary/per_day_rate/monthly_salary = null`; admin sees real figures.
- DEFERRED to R27.6: advance *settlement* UI is still the prompt-based reconcile from R27.4 (functional, not the richer issue/settle screen); daily attendance has the per-month form (the `attendance_daily` table + bulk calendar UI is migrated but not surfaced).

## #9 Admin sidebar redesign — PASS
- `AdminLayout.tsx`: items grouped into Overview / Sales / Procurement / Inventory / Logistics / Web Shop / Finance / People / Marketing / System, each a collapsible section with a chevron + lucide icons.
- Live search box filters items (flattens matches across groups). Expand state persisted in `localStorage` (`adminNavExpanded`); defaults to only Overview + Sales expanded.
- Role filtering (`ROLE_PAGES`) preserved — non-admin roles still see only their allowed items.

---

## Migrations (additive only, per rule)
`runR27_5Migrations()` ran cleanly on a copy of live `data.db` — every `[migrations] R27.5: … added/ok` logged, `complete` reached. No drops/renames.

## Build artifacts
- `dist/public` rebuilt; `narmada-mobility-godaddy.zip` regenerated (66 files, maps excluded).
- `config.js` confirmed `window.__API_BASE__ = "https://narmada-backend.onrender.com"`.

## Not done / caveats (honest)
- Visual browser pixel-testing of the header at specific widths was not possible headlessly (verified via CSS/breakpoints + build).
- #5 full dispatch→store flow not driven through a real PO (no PO rows in the available DB); the storage + route wiring is verified independently.
- #8 advance-settlement screen + attendance bulk calendar deferred to R27.6 (table + endpoints exist).
