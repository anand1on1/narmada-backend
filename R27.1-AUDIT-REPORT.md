# R27.1 — Audit Report

Date: 2026-06-21
Build root: /tmp/narmada-fresh

## 1. Type check
`npx tsc --noEmit -p .` → **0 errors**.

## 2. Build
`npm run build` → client (vite) + server (esbuild → dist/index.cjs) built OK.
Deliverable: `narmada-mobility-godaddy.zip` (from dist/public, `*.map` excluded; none emitted).

## 3. Local server boot (PORT=5001)
All R27.1 migrations logged `ready`; `[migrations] R27.1: complete`; `[boot] listening on PORT 5001`.
No unhandled exceptions in the run log.

## 4. Endpoint curl audit (live, port 5001)
Legend: ✅ = expected response.

### Public / shop
| Endpoint | Method | Result |
|---|---|---|
| /api/public/currencies | GET | ✅ `{default:"INR",available:[INR,USD],rates:{USD:{rate_inr:83.5,fallback:true}}}` |
| /api/shop/signup | POST | ✅ token + user (id 1) |
| /api/shop/login | POST | ✅ token |
| /api/shop/me | GET | ✅ user |
| /api/shop/addresses | POST/GET | ✅ created + listed |
| /api/shop/freight-quote | POST | ✅ 0 before rule, 500 after X-100 rule (250×2) |
| /api/shop/orders | POST | ✅ NM/ORD/26/0001, total 1000, COD/pending/placed |
| /api/shop/orders, /:id | GET | ✅ list + detail |
| /api/shop/logout | POST | ✅ `{ok:true}` |

### Admin
| Endpoint | Method | Result |
|---|---|---|
| /api/admin/shop-orders | GET | ✅ total 1 |
| /api/admin/shop-orders/:id | GET | ✅ detail |
| /api/admin/shop-orders/:id/status | PATCH | ✅ confirmed |
| /api/admin/shop-orders/:id/dispatch | PATCH | ✅ DTDC/DKT123 |
| /api/admin/shop-customers | GET | ✅ orderCount 1, totalSpend 1000 |
| /api/admin/freight-charges/:partNumber | PATCH | ✅ upsert X-100=250 |
| /api/admin/freight-charges | GET | ✅ rows |
| /api/admin/freight-charges/bulk | POST | ✅ `{updated:2}` (payload `{part_numbers:[],freight_inr}`) |
| /api/admin/freight-charges/csv | POST | ✅ `{ok:true,upserted:2}` |
| /api/admin/purchase-orders/:id/duplicate | POST | ✅ clean 404 on missing id (no crash) |
| /api/admin/quotations/:id/duplicate | POST | ✅ clean 404 on missing id (no crash) |

### Regression spot-checks (existing endpoints)
| Endpoint | Result |
|---|---|
| /api/admin/me | ✅ admin role |
| /api/admin/chats | ✅ list returned |

Products/Delhi-PO routes live in other route files (not touched this release); their
paths returned the SPA shell only because the exact literal differs — feature unaffected.

## 5. Scope note — routes-v2.ts structure
R27.1 routes are registered inside `registerR8Routes` (the function that owns `app`,
`requireAuth`, `ctx`, `uploadsRoot`, `multerPO`). `docStore` is not in that scope, so a
local `freightCsvStore` multer instance was added for the CSV upload. `v2` and
`sendGenericEmail` are module-level and used directly.

## 6. Open items (deferred per fallback rule — NOT silently dropped)
- **TASK 9 chat attachments**: only the DB columns
  (`customer_chat_messages.attachment_url`, `attachment_type`) were added
  (`server/migrations.ts:2196` area). The AiSensy media-send helper, the 90%
  send-failure investigation, and the compose-attachment UI are **not implemented**.
  Recommended next step: add a media branch in the existing chat-send handler in
  `server/routes-v2.ts` (R9 chat route) and a file picker in the admin chat composer.
